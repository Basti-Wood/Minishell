/* Updated t_cmd structure - Replace this in your minishell.h file */

typedef struct s_cmd
{
	char			**argv;
	t_redir			*redirs;      /* Single list for all redirections in order */
	int			heredoc;      /* File descriptor for heredoc if exists */
	struct s_cmd		*next;
}				t_cmd;

/* 
 * REMOVE these lines from your header:
 * - t_redir *infiles;
 * - t_redir *outfiles;
 * 
 * REMOVE these function declarations:
 * - void add_to_infiles(t_cmd *cmd, t_redir *redir);
 * - void add_to_outfiles(t_cmd *cmd, t_redir *redir);
 * - int handle_input_redirections(t_cmd *cmd);
 * - int handle_output_redirection(t_cmd *cmd);
 */
